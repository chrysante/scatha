// No include guards

// ===--------------------------------------------------------------------=== //
// === List of all reasons for a generic bad declaration -----------------=== //
// ===--------------------------------------------------------------------=== //

#ifndef SC_SEMA_GENERICBADDECL_DEF
#   define SC_SEMA_GENERICBADDECL_DEF(reason, severity, text)
#endif

SC_SEMA_GENERICBADDECL_DEF(ReservedIdentifier,
                           Error,
                           "Reserved identifier")
SC_SEMA_GENERICBADDECL_DEF(InvalidInScope,
                           Error,
                           declaration()->nodeType() << " invalid in "
                               << scope()->kind() << " scope")

#undef SC_SEMA_GENERICBADDECL_DEF

// ===--------------------------------------------------------------------=== //
// === List of all reasons for a bad variable declaration ----------------=== //
// ===--------------------------------------------------------------------=== //

#ifndef SC_SEMA_BADVARDECL_DEF
#   define SC_SEMA_BADVARDECL_DEF(reason, severity, text)
#endif

SC_SEMA_BADVARDECL_DEF(IncompleteType,
                       Error,
                       "Cannot declare variable of incomplete type " << type()->name())
SC_SEMA_BADVARDECL_DEF(ExpectedRefInit,
                       Error,
                       "An lvalue expression is required to initialize variable reference")
SC_SEMA_BADVARDECL_DEF(CantInferType,
                       Error,
                       "Can't infer type")

#undef SC_SEMA_BADVARDECL_DEF

// ===--------------------------------------------------------------------=== //
// === List of all reasons for a bad function definition -----------------=== //
// ===--------------------------------------------------------------------=== //

#ifndef SC_SEMA_BADFUNCDEF_DEF
#   define SC_SEMA_BADFUNCDEF_DEF(reason, severity, text)
#endif

SC_SEMA_BADFUNCDEF_DEF(InvalidSMF,
                       Error,
                       "Invalid special member function declaration")

#undef SC_SEMA_BADFUNCDEF_DEF
